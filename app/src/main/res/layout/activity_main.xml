<?xml version="1.0" encoding="utf-8"?>
<!--- activity_main.xml
    @version(Original)                      @author(Travis Moretz)
    View for testing a passkey against a saved passkey.
    @version(2016.1.5)                      @editor(Cameron Dziurgot)
    Put grid and button in a separate layout and include it in this layout. -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:id="@+id/clickLayout"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context=".activity_main"
    android:background="#FFF">

    <include layout="@layout/content_grid" />
</LinearLayout>
